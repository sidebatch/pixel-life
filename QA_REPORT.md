# Pixel Life 종합 QA 보고서

검증일: 2026-09-23

대상: 생활 스킬 성장·UI 개편 로컬 빌드와 커밋 `254443a`의 공개 Pages 빌드

지원 기준: Android 세로 화면, 키보드 보조 입력, GitHub Pages

## 결론

월드 이동, 시간·날씨 미리보기, 낚시 시작·취소·획득, 물고기 도감, 낚싯대 장비, 로컬 저장·복원과 Pages 배포 절차를 다시 검증했다. 치명적 오류나 콘솔 오류는 발견되지 않았다.

이번 변경의 390×844 로컬 모바일 QA에서는 도감 카드→중앙 정보 팝업→뒤로가기 복귀, 장비 화면의 XP 바와 한국어 사용 조건, 실제 낚시 결과의 +XP→72/72 XP(100%)→Lv.2·8/84 XP 전환을 확인했다. `254443a`의 GitHub Pages 배포도 성공했고, 공개 주소에서 새 HUD·장비 경험치 바·도감 팝업·뒤로가기를 재확인했다. 공개 페이지의 브라우저 오류·경고 로그는 0건이었다.

QA 중 낚시 결과창이 열린 순간 메뉴 버튼이 다시 활성화될 수 있는 상호작용 문제를 발견했다. 캐스팅부터 결과창을 닫을 때까지 가방·메뉴·설정 버튼을 실제 `disabled` 상태로 유지하도록 수정했다. CSS 포인터 차단뿐 아니라 키보드와 접근성 입력도 함께 차단한다.

## 자동 검사

`node scripts/check.mjs`가 다음 항목을 검사한다.

- 22개 런타임 스크립트의 통합 구문
- JavaScript가 참조하는 HTML ID의 존재 여부와 ID 중복
- 도감 필터 4개의 탭·탭패널 접근성 구조
- 런타임 이미지 54개의 존재 여부
- 물고기 20종 ID·에셋 키 중복, 크기·가격·XP·Weight 유효성
- 물고기 PNG 서명과 96×96 크기
- 연못·강·바다의 모든 시간대·날씨 조합에 최소 한 종 이상 존재
- 시간 파서와 `DAWN / DAY / DUSK / NIGHT` 경계값
- 가중치 추첨의 양 끝값과 3회 연속 등장 페널티·해제
- 낚시 XP 레벨업, Lv.20→21·Lv.100→숙련도 경계, 구 Lv.20 저장의 누적 XP 복원
- 낚싯대 5종의 순서·기본 장비·효과 수치와 해금 조건
- 장비별 입질 대기 감소, 희귀 이상 Weight 증가와 큰 개체 보정
- 발견 수, 최소·최대·평균 크기 누적
- 5·10·15·19·20종 도감 보상과 중복 지급 방지
- 희귀·영웅·전설 효과 설정, 단계별 파티클 증가와 전설 합성음 11개 음원 예약
- 디버그 지정 어종 강제 선택
- 버전형 저장·복원, 특별 낚싯대와 장착 장비 복원, 잠긴 장비 저장값의 안전한 보정
- 손상된 JSON 저장과 알 수 없는 저장 버전의 안전한 거부
- Lv.100 숙련도·장착 낚싯대 저장 복원
- 64×48 월드 좌표·충돌·건물·NPC 배치 오류

`node scripts/build.mjs`는 CSS·JavaScript·이미지를 `dist/index.html` 하나로 합치고 외부 런타임 의존성이 남지 않았는지 검사한다.

## 실제 모바일 배포본 QA

GitHub Pages 배포본을 412×915 Android 세로 뷰포트에서 직접 조작했다.

| 영역 | 검증 내용 | 결과 |
|---|---|---|
| 공개 진입 | 일반 링크에서 디버그 패널이 숨겨짐 | 통과 |
| 시간 | 새벽 06:30, 낮 12:00, 저녁 18:30, 밤 22:00, ±30분 | 통과 |
| 날씨 | 맑음·비·폭풍 버튼, HUD 아이콘, 화면 전체 강수 | 통과 |
| 이동 | 모바일 조이스틱 이동, 물가 충돌, 방향 전환 | 통과 |
| 낚시 시작 | 물을 바라볼 때 `낚시`, 캐스팅·대기·입질 전환 | 통과 |
| 낚시 취소 | 취소 후 메뉴가 열리지 않고 버튼 상태가 복원됨 | 통과 |
| 낚시 결과 | 전용 이미지, 등급, 크기, 가격, XP, `NEW` 표시 | 통과 |
| 도감 | 20종 표시와 전체 20·연못 6·강 7·바다 7 필터 | 통과 |
| 도감 힌트 | 15종 전 희귀어의 정확한 조건이 숨겨짐 | 통과 |
| 도감 보상 | 다음 보상, 발견 수, 진행 막대 표시 | 통과 |
| 저장 | 획득 후 새로고침해 발견 수·크기 통계가 복원됨 | 통과 |
| 희귀 결과 | `RARE CATCH`, 파란 파티클 16개, 상승음 | 통과 |
| 영웅 결과 | `HEROIC CATCH`, 보라 파티클 22개, 확장 상승음 | 통과 |
| 전설 결과 | `LEGENDARY CATCH`, 금빛 파티클 30개, 화음·저음 | 통과 |
| 일반 회귀 | 일반 물고기에 상위 등급 효과가 표시되지 않음 | 통과 |
| 낚시 장비 | 390×844에서 장비 창, 기본 장착, 4개 잠금 조건과 효과 표시 | 통과 |
| 장비 레이아웃 | 전체 화면 패널, 내부 스크롤, 현재 장비 상세가 화면 안에 유지됨 | 통과 |
| 런타임 | 브라우저 오류·경고 로그 | 0건 |

## 이번 변경의 로컬 모바일 QA

| 영역 | 390×844 검증 내용 | 결과 |
|---|---|
| 상단 HUD | 마을 이름·시간·날씨, 메뉴 버튼 정렬 | 통과 |
| 도감 팝업 | 발견 물고기 정보가 화면 중앙에 표시되고 뒤로가기로 목록 복귀 | 통과 |
| 낚시 장비 | 68/72 XP 바, 다음 목표와 잠긴 장비의 사용 조건 확인 | 통과 |
| 실제 낚시 | 디버그 지정 어종으로 40 XP 획득, 두 번째 획득에서 100%→Lv.2 전환 | 통과 |
| 런타임 | 로컬 게임 브라우저 오류·경고 로그 | 0건 |

## 결과창·효과음 후속 보정 QA

390×844 로컬 게임에서 결과창의 제목을 `어종 (등급)`으로 바꾸고 등급별 굵은 글자색을 확인했다. 장착 낚싯대·`다음 목표` 줄은 결과창에서 제거했고, 장비 경험치 카드에서도 `다음 목표`를 숨겼다. 레벨업 알림은 3초 시점에 남아 있고 약 5초 뒤 자동 종료되며, X·브라우저 뒤로가기·결과창 닫기로 즉시 종료된다. 결과창을 빠르게 닫아도 알림이 다시 뜨지 않았고 브라우저 오류·경고는 0건이었다.

물 첨벙·획득 성공음은 기존 Web Audio 효과음 체계에서 생성한다. 자동 검사로 두 효과음의 노이즈·음계 재생 예약을 확인했으며, 실제 Android 기기 스피커에서의 음량·음색은 후속 청음이 필요하다.

후속 조정: 투척음은 줄 스침 뒤 캐스팅 시간(약 0.32초)에 맞춰 물 충돌·잔물결이 들리도록, 획득음은 릴 당김·수면 튐·꼬리치기 순서로 구분했다. 희귀어 축하음은 물소리 뒤에 시작한다. 결과창과 도감 목록에서는 등급 단어를 숨기고 이름의 색·굵기를 강조했으며, 도감 상세 팝업의 등급 텍스트는 유지했다. 390×844 로컬 화면에서 금붕어·황금잉어·실러캔스의 색과 상세 등급을 확인했고 오류·경고 로그는 0건이었다.

## 에셋 QA

- 물고기 20종이 데이터의 에셋 키와 일대일로 연결된다.
- 게임용 이미지는 모두 96×96 투명 PNG다.
- 도감 카드·상세 화면·낚시 결과가 동일한 어종 이미지를 사용한다.
- 전체 접촉 시트에서 잘림, 불투명 사각 배경, 어종 중복을 확인하지 못했다.
- 광어는 실제 넙치류처럼 양쪽 눈이 몸의 한쪽 면에 모인 형태이며, 현재 이미지도 그 특징을 따른다.

## 저장과 호환성

- 저장 키: `pixel-life.save`
- 저장 버전: `1`
- 저장 대상: 물고기 인벤토리, 발견·크기 통계, 코인, 낚시 누적 XP·레벨·숙련도, 도감 보상 플래그, 특별 낚싯대, 장착 낚싯대 ID
- 구 Lv.20 상한 시절 저장의 누적 XP를 재계산해 초과 획득분을 버리지 않는다.
- 현재 브라우저의 로컬 저장만 사용하며 기기·브라우저 간 동기화는 지원하지 않는다.
- 손상되거나 지원하지 않는 버전의 저장은 적용하지 않고 기본 상태로 계속 실행한다.

## 현재 남은 범위

다음 항목은 결함이 아니라 아직 구현되지 않은 기능이다.

- 강·바다 실제 월드와 낚시터
- 인벤토리 실제 아이템 UI, 판매와 요리
- 날씨 전환 페이드와 환경음
- 세로 9:16보다 긴 화면의 상·하단 여백 최적화

## 배포 전 필수 명령

```bash
node scripts/check.mjs
node scripts/build.mjs
git diff --check
```

`main` 푸시 후 GitHub Actions의 `Deploy GitHub Pages` 성공과 공개 URL의 최신 빌드 응답까지 확인한다.
