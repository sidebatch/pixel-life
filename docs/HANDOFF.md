# Pixel Life 개발 인수인계

최종 정리일: 2026-09-24

기준 브랜치: `main`

공개 저장소: https://github.com/sidebatch/pixel-life

공개 게임: https://sidebatch.github.io/pixel-life/

이 문서가 다음 개발 세션의 시작점이다. 먼저 이 문서를 읽고, 생활 스킬 성장의 확정 설계 기준은 `docs/LIFE_SKILL_PROGRESSION.md`, 세부 낚시 사양과 현재 구현 기록은 `docs/FISHING_PLAN.md`, 구조 원칙은 `docs/ARCHITECTURE.md`에서 확인한다. 이후 사용자가 변경을 요청하면 성장 설계도 갱신할 수 있다.

성장 설계와 현재 코드의 관계: 낚시는 독립 Lv.1–100과 Lv.100 이후 숙련도, 획득 직후 +XP·가로 경험치 바·레벨업·새 장비 사용 가능 안내를 연결했다. Lv.1–20의 기존 XP 요구량은 저장 호환을 위해 유지했고, Lv.21 이후 곡선과 숙련도 요구량은 임시 수치다. `src/data/life-skill-data.js`에 6개 스킬의 공통 메타데이터를 준비했고 벌목·농사의 기본 행동과 저장은 구현했으나, 두 활동의 스킬 XP·레벨 UI는 아직 연결되지 않았다. 설계 문서의 예시 XP와 보상은 확정 밸런스나 이미 구현된 콘텐츠로 간주하지 않는다.

## 1. 세션 시작 체크리스트

```bash
git status --short
git pull --ff-only origin main
node scripts/check.mjs
node scripts/build.mjs
node scripts/serve.mjs
```

개발 서버는 `http://127.0.0.1:4173`에서 열린다. `dist/index.html`은 생성 파일이므로 직접 수정하거나 커밋하지 않는다.

스크립트 파일을 추가할 때는 반드시 다음 세 곳의 순서를 함께 맞춘다.

- `index.html`
- `scripts/check.mjs`
- `scripts/build.mjs`

## 2. 현재 구현 상태

### 월드 기반

- 64×48 라일락 연못 마을
- 플레이어 이동·충돌·카메라
- PC 연속 이동 보간과 모바일 픽셀 스냅 분리
- NPC 6명 로밍·대화
- 건물 2채, 입구 상호작용, Y축 가림
- 연결형 길, 물, 식생 레이어, 월드 배치 자동 검사
- 모바일 세로 HUD와 조이스틱
- 마을 북쪽 출구 ↔ 오래된 숲, 동쪽 출구 ↔ 햇살 농장. 각 지역은 별도 64×48 맵이며 이동 위치와 현재 지역을 저장한다.
- 숲에는 모든 나무가 벌목 가능하며 강 낚시터가 있다. 농장에는 4×4 고정 밭과 연못 낚시터가 있다.
- 숲의 8종 나무·그루터기·종류별 통나무 PNG와 당근·감자·옥수수·딸기 씨앗·다 자란 밭 작물·수확물 PNG를 적용했다. 상세 규격·원본 ZIP 처리 기록은 `docs/LIFE_ASSET_STANDARD.md`를 참고한다.

### 벌목·농사 기본 루프

- 공용 근접 대상 판정으로 출구·NPC·벌목 나무·밭·낚시를 구분한다. 농장의 가까운 밭은 화면 터치로도 선택할 수 있다.
- 숲의 모든 나무는 세 번 때리면 해당 종의 그루터기가 되고 같은 종의 통나무 2~4개를 준다. 현실 시간 5분 후 게임을 꺼두어도 다시 자란다. 숲 가장자리 나무도 벌목 가능하지만 맵 바깥 경계는 계속 막힌다.
- 밭은 처음 4칸을 사용하고 나머지 12칸은 하나씩 코인(후반부는 통나무도 사용)으로 영구 확장한다. 비용은 `src/data/life-content-data.js`에 둔다.
- 엘리 노점에서 당근·감자·옥수수·딸기 씨앗을 구매해 빈 밭에 심는다. 각각 현실 시간 2·5·15·30분 뒤 물 주기 없이 수확 가능하며 오프라인 경과 시간도 반영한다.
- 씨앗을 심은 직후부터 수확 가능할 때까지 밭에 각 작물의 동일한 전용 그림을 크기·투명도 단계로 그린다. 밭 바탕과 잠긴 칸도 작물 그림의 흙 색감에 맞춘다. 성장·수확 안내에서 다른 스타일의 이모지는 사용하지 않는다.
- 수확 작물과 종류별 통나무는 물고기처럼 노점에서 수량을 선택해 판매한다. 가방의 `재료` 탭에서 씨앗·수확물·통나무를 각자의 이미지로 확인한다. 새 통나무는 기존 통나무와 동일한 개당 12코인이며, 밭 확장에는 기존·종류별 통나무를 합쳐 사용할 수 있다. 이전 저장의 일반 통나무도 보존한다. 거래·밭·벌목 변경은 저장 실패 시 되돌린다.

### 월드 시간

- 게임 하루는 현실 30분
- `DAWN / DAY / DUSK / NIGHT` 판정
- 새벽·저녁의 따뜻한 그라데이션
- 밤의 화면 색조, 플레이어 주변 비네팅, 가로등·건물 조명
- 갑작스러운 단색 전환 대신 시간 구간 사이 보간

### 날씨

- `clear / rain / storm`
- 게임 시간 6시간 단위 자동 판정
- 맑음 65%, 비 27%, 폭풍 8%
- 하루 종일 건조하지 않도록 약한 비 보정
- 화면 전체에 독립 분포되는 2겹 비와 3겹 폭풍
- 전체 화면 습윤 색조, 빗방울 파문, 폭풍 번개·플래시

### 낚시 수직 슬라이스

- 물을 바라보고 상호작용하면 낚시 시작
- 캐스팅 0.32초 → 기본 낚싯대 기준 랜덤 3~6초 대기 → 입질 → 버튼 입력 → 100% 획득. 장착 낚싯대의 대기 감소 효과는 이 범위에 적용된다.
- 낚싯줄, 찌, 입질 `!` 연출. 투척 중·입질 대기 중·입질 시 상황 설명 문구는 숨기고, 입질은 효과음과 찌 옆의 `!`로만 알린다. 취소·상호작용 버튼은 유지한다.
- 공식 20종 물고기를 `src/data/fish-data.js`에 데이터로 분리
- 현재 지역·시간대·날씨 조건으로 Fish Pool을 필터링한 뒤 상대 Weight로 추첨
- 같은 어종이 3회 연속 나오면 다음 추첨부터 해당 Weight를 절반으로 낮추고, 다른 어종이 나오면 해제
- 새 바닷가 맵과 지역별 어종 밸런스가 정리되기 전에는 마을 연못에서 20종 모두 등장할 수 있다. `src/fishing.js`의 `FISHING_CONFIG.temporaryAllFishAtVillagePond`가 마을에서만 지역 조건을 임시로 풀며 시간·날씨·Weight는 유지한다. 숲의 강과 농장 연못은 각자의 출현 풀을 사용한다. 바닷가 확장 시 이 플래그와 도감 임시 안내를 다시 검토한다.
- 모든 어종에 공통 크기·가격 계산 적용
- 어종별 낚시 XP를 누적하고 Lv.100 및 이후 숙련도까지 계산 (Lv.21 이후 요구량은 임시 밸런스)
- 첫 발견, 획득 수, 최소·최대·평균 크기를 `GAME_STATE.collections.fish`에 기록
- 저장 버전 v1로 코인·Fishing 진행도·인벤토리·발견 통계를 `localStorage`에 저장·복원
- 결과 카드에 등급·크기·가격·경험치 바를 표시하고, +XP·100% 채움·레벨업을 별도 피드백으로 표시
- 낚시 결과창은 모바일 게임 화면의 중앙 부근(높이 56%)에 배치하고, `확인` 버튼을 우하단에 고정한다. 작은 화면에서는 내용 영역만 스크롤하며 희귀도 등장 애니메이션도 창의 중앙 정렬을 유지한다.
- 결과창 제목은 어종 이름만 굵고 크게 표시하고 등급별 글자색을 적용. 등급 글자는 결과창과 도감 카드에 표시하지 않고, 도감 상세에서 확인 가능
- 레벨업 알림은 약 5초 유지되며 X·취소키·Android 뒤로가기로 즉시 닫힘
- 낚시 투척·입질·획득에는 사용자가 제공한 MP3를 각각 `assets/fishing/audio/cast.mp3`, `bite.mp3`, `catch.mp3`로 연결하고, 레벨업에는 `assets/audio/level-up.mp3`, 상점 판매 성공에는 `assets/audio/market-sale.mp3`를 연결한다. 모바일 재생 지연 완화를 위해 다섯 음원을 로딩 시 Web Audio 버퍼로 미리 디코딩하고 첫 조작에서 오디오 컨텍스트를 활성화한다. 미지원·로딩 실패 시 기존 MP3 플레이어로 재생한다. 단일 HTML 빌드에도 다섯 음원을 포함하며 희귀도 축하음은 별도 Web Audio 합성음으로 유지한다.
- 첫 발견은 `NEW` 배지와 도감 등록 문구로 구분
- 월드 메뉴의 물고기 도감에서 전체 20종과 전체·연못·강·바다 필터 제공
- 도감의 연못·강·바다 탭과 상세 서식지는 원래 분류를 유지하며, 상단에 모든 어종의 마을 연못 임시 출현 안내를 표시한다.
- 발견 어종은 설명·등급·획득 수·최소·최대·평균 크기·출현 조건 표시
- 도감 카드를 누르면 중앙 상세 팝업이 열리고, X·뒤로가기로 목록에 복귀
- 미발견 어종은 `???` 카드와 서식지·시간·날씨 힌트 표시
- 20종 전용 96×96 투명 픽셀 아트를 도감 카드·상세 화면·낚시 결과에 연결
- 생성 원본은 `assets/fishing/source/`, 게임용 정규화 파일은 `assets/fishing/`에 분리
- 도감 5종은 코인 300, 10종은 Fishing XP 240, 15종은 희귀어 상세 힌트, 19종은 마지막 단서, 20종은 강태공 칭호와 특별 낚싯대 지급
- 도감 상단에 다음 보상과 진행 막대를 표시하며, 보상은 저장 플래그로 한 번만 자동 지급
- 15종 전에는 미발견 희귀 이상 어종의 정확한 시간·날씨를 숨기고, 19종 달성 후 마지막 한 종의 정확한 단서를 공개
- 희귀·영웅·전설 결과에 각각 16·22·30개의 등급색 파티클, 등장 배너·카드 애니메이션과 상승형 합성 효과음 적용
- `prefers-reduced-motion`에서는 방사형 모션을 숨기고 등급 배너만 유지
- `?debug&fish=fish.catfish`처럼 지정하면 조건·지역과 관계없이 해당 어종이 즉시 입질하여 결과 연출을 반복 검증할 수 있음
- 기본·튼튼한·강철·전문가·강태공의 낚싯대 5종과 Fishing Lv.1/5/10/15·도감 20종 해금 조건 연결
- 월드 메뉴의 낚시 장비 화면에서 경험치 바·다음 목표·잠금 조건·쉬운 효과 문구·현재 장비를 확인. 잠긴 장비도 내용을 볼 수 있지만 장착은 불가
- 장착 장비의 입질 대기 감소, 희귀 이상 Weight 증가, 큰 개체 보정을 실제 낚시 결과에 적용
- 장착 낚싯대 ID를 저장·복원하고 유효하지 않거나 잠긴 저장값은 기본 낚싯대로 보정
- 획득 물고기를 `GAME_STATE.inventory`에 한 마리씩 추가하고 저장·복원
- 월드 메뉴의 `가방`에서 물고기는 3열 카드형 바둑판으로 표시한다. 각 이미지 오른쪽 위에 보유 수를 숫자로 표시하고, 이름은 PC 커서 올리기·키보드 포커스·모바일 터치 때만 보여 준다. 개체별 크기·판매가는 화면에 노출하지 않지만 저장 데이터에는 유지한다. `장비` 탭에는 현재 사용 가능한 낚싯대와 장착 상태를 표시한다. 판매·장착 변경은 가방에서 하지 않는다.
- 연못 서쪽의 엘리 옆에 2타일 오픈형 노점을 두고, 엘리는 노점 앞에 고정한다. 물고기 판매는 월드 메뉴나 가방이 아니라 엘리를 바라보고 상호작용할 때만 연다.
- 판매창에서 어종별 `−`·`+`·`전부`로 수량을 선택하고 예상 합계를 확인한다. 하단에는 `나가기`와 `선택한 물고기 판매` 버튼을 나란히 두며, `나가기`는 상단 X·뒤로가기와 같은 닫기 경로를 사용한다. 같은 어종의 판매 개체는 낚은 순서가 빠른 것부터 정하며, 각 개체의 저장 가격을 합산한다. 판매 확정 시 선택한 물고기만 차감하고 코인을 올려 저장한다. 저장 실패 시 둘 다 되돌린다.
- 판매 성공 후 판매창 상단 보유 코인과 월드 HUD 코인이 약 0.9초에 걸쳐 증가하고, `+금액` 배지와 사용자 제공 돈 소리가 재생된다. 저장 실패 시 소리·증가 연출은 실행하지 않는다. 모션 감소 설정에서는 숫자를 즉시 확정한다. 판매창을 닫거나 다시 열 때 이전 판매의 배지·코인 강조 효과를 지우고 최종 코인 숫자로 맞춰, 재진입 시 과거 판매 연출이 다시 나타나지 않게 한다.
- 화면에 표시되는 돈에서 `G` 접미사를 사용하지 않는다. 상점 보유 코인·가격·합계·판매 완료·증가 배지, 낚시 결과 판매가, 도감 보상 안내에 적용하며 내부 코인 값과 저장 형식은 그대로다.
- 새로고침 후에도 코인·남은 물고기가 유지되며, 판매해도 도감 발견 기록은 보존한다. 상점은 씨앗 구매와 물고기·통나무·작물 판매를 지원한다. 미끼 등 추가 품목과 요리는 아직 없다.
- 모바일 `낚시 취소` 버튼 제공
- 취소 버튼은 조작 버튼 위에 표시되며, 취소는 `click` 시점에 처리해 모바일 고스트 클릭을 막는다.
- 낚시 중 메뉴 버튼은 보이지만 흐리게 표시되고 터치는 비활성화된다.
- 캐스팅부터 결과창을 닫을 때까지 메뉴 버튼은 실제 `disabled` 상태로 유지되어 키보드·접근성 입력으로도 겹침 메뉴가 열리지 않는다.

## 3. 아직 구현되지 않은 항목

낚시가 완성된 상태는 아니다. 현재 코드는 전체 시스템을 검증하기 위한 첫 수직 슬라이스다.

- 바닷가 실제 월드와 낚시터, 임시 마을 전체 어종 출현 정책 정리
- 씨앗 외 추가 구매 품목과 요리
- 벌목·농사의 스킬 XP·레벨, 전용 도구·애니메이션
중요: 저장은 현재 브라우저의 `pixel-life.save` 키에만 유지된다. 도감은 저장된 발견·크기 통계를 읽어 표시한다.

시간·날씨도 다음 항목은 남아 있다.

- 날씨 상태가 바뀔 때의 페이드 전환
- 비·폭풍 사운드와 밤 환경음
- 젖은 지면·물웅덩이·바람 반응
- 계절 시스템

## 4. 다음 권장 작업 순서

1. 실제 Android에서 숲·농장 이동, 밭 터치 대상, 상점 스크롤·버튼을 확인하고 게임 경제·작물 시간·나무 재생 시간을 플레이 테스트로 조정한다.
2. 벌목·농사에 공통 생활 스킬 XP·레벨 UI를 단계적으로 연결한다. 현 버전은 행동·보상·저장만 구현했다.
3. 바닷가 지역과 어종별 지역 제한을 완성하고, 마을 연못의 임시 전체 출현 설정을 제거한다. 선택형 미끼 등 새 구매 품목과 요리는 별도 설계한다.

20종 데이터, Fish Pool 선택, 3연속 보정, XP·발견 통계·저장·복원, 도감 UI·전용 픽셀 아트, 5단계 도감 보상, 상위 3등급 결과 연출과 낚싯대 5종의 선택·효과를 검증했다. 가방은 물고기 이미지의 3열 바둑판과 보유 수 배지로 표시한다. 엘리 노점의 판매·코인 저장·돈 소리와 약 0.9초 증가 연출도 확인했다. 2026-09-24에는 마을과 연결된 숲·농장, 벌목·밭 확장·씨앗 구매·심기·오프라인 성장·수확·자원 판매를 로컬 게임 화면에서 검증했다. 실기기 Android 확인은 남아 있다.

## 5. 주요 파일

| 파일 | 책임 |
|---|---|
| `src/data/world-map.js` | 맵·지형·건물·NPC 배치 원본 |
| `src/data/region-maps.js` | 마을·숲·농장 연결, 새 지역 배치와 출입구 |
| `src/data/life-content-data.js` | 벌목·밭 확장·작물 성장·가격 설정 |
| `docs/LIFE_ASSET_STANDARD.md` | 숲 나무·그루터기·통나무·씨앗의 이미지 규격과 적용 범위 |
| `src/data/fish-data.js` | 20종 물고기, 서식지·시간·날씨·Weight·보상 데이터 |
| `src/data/fishing-gear-data.js` | 낚싯대 5종, 레벨·도감 해금 조건과 효과 데이터 |
| `src/data/life-skill-data.js` / `src/life-skills.js` | 공통 생활 스킬 메타데이터와 Lv.1–100·숙련도 성장 계산 |
| `src/skill-ui.js` | 공통 스킬 경험치 카드, +XP·100%·레벨업 연출 |
| `assets/fishing/*.png` | 도감·결과 카드용 20종 96×96 투명 픽셀 아트 |
| `assets/fishing/source/*.png` | 재가공을 위한 이미지 생성 원본 |
| `scripts/process-fish-assets.py` | 원본 알파 정리, 96×96 정규화와 검토 시트 생성 |
| `src/world-time.js` | 30분 월드 시계, 시간대, 디버그 시간 UI |
| `src/weather.js` | 날씨 확률·구간·디버그 고정 |
| `src/fishing.js` | 낚시 상태 머신, Fish Pool·Weight 선택, XP·발견 통계와 도감 보상 지급 |
| `src/fishing-effects.js` | 투척·입질·획득·레벨업 MP3 재생 및 희귀·영웅·전설 결과 파티클·배너 애니메이션·합성 효과음 |
| `src/fish-dex.js` | 도감 필터, 발견 카드, 단계별 힌트·보상 진행도와 크기 통계 UI |
| `src/fishing-gear.js` | 낚싯대 잠금·장착·상세 효과 UI와 즉시 저장 |
| `src/inventory.js` | 가방의 물고기 카드·보유 수 배지·이름 터치 노출과 사용 가능한 낚싯대 표시; 개별 기록은 저장 데이터에 유지 |
| `src/market.js` | 엘리 노점의 수량 선택·개체별 가격 합산·판매 거래·저장 실패 롤백·코인 증가 연출 |
| `src/life-content.js` | 벌목, 밭 선택·확장·심기·수확과 자원 획득 |
| `src/save.js` | 저장 버전 v1, 기존 낚시 데이터와 지역·위치·밭·나무·씨앗·작물·통나무 검증과 복원 |
| `src/simulation.js` | 이동·충돌·NPC·카메라와 낚시 중 이동 잠금 |
| `src/rendering.js` | 월드·시간·날씨·찌와 오픈형 노점 렌더링 |
| `src/interactions.js` | 모바일/키보드 입력, 취소 버튼, 대화·메뉴 |
| `src/config.js` | 공용 `GAME_STATE`, Fishing 진행도·발견 기록, 이동 속도, 모듈 목록 |
| `styles/game.css` | 세로 HUD, 낚시 취소·도감·낚시 장비·가방·상점 UI |

## 6. 개발자 테스트

`?debug`가 있으면 게임 화면에 시간·날씨 조절 패널이 표시된다.

```text
?debug&time=06:30&weather=clear   새벽
?debug&time=12:00&weather=rain    낮+비
?debug&time=18:30&weather=rain    저녁+비
?debug&time=22:00&weather=storm   밤+폭풍
```

예시: https://sidebatch.github.io/pixel-life/?debug&time=22:00&weather=storm

키보드 보조 기능:

- `F3`: 월드 충돌·배치 디버그
- `[` / `]`: 디버그 상태에서 30분 단위 시간 조절
- 방향키/WASD: 이동
- Space/Z: 상호작용
- X/Escape: 취소

모바일 낚시 QA:

1. 물가를 바라볼 때 상황 문구가 `낚시`인지 확인한다.
2. 손 버튼을 누르면 찌와 낚싯줄이 표시되는지 확인한다.
3. `낚시 취소`가 조작 버튼 위쪽에 보이는지 확인한다.
4. 메뉴 버튼은 보이되 흐리고 눌리지 않는지 확인한다.
5. 취소 후 메뉴가 함께 열리지 않는지 확인한다.
6. 입질 후 손 버튼으로 붕어 결과가 100% 표시되는지 확인한다.
7. 메뉴의 `도감`을 열어 발견 수와 발견 어종 통계가 최신인지 확인한다.
8. 전체·연못·강·바다 탭 전환과 미발견 어종 힌트를 확인한다.
9. 도감을 닫은 뒤 이동과 낚시가 정상적으로 다시 가능한지 확인한다.
10. 발견 어종 카드·상세 화면·낚시 결과에서 같은 전용 물고기 이미지가 표시되는지 확인한다.
11. 도감 상단에 다음 보상 목표와 현재 진행도가 표시되는지 확인한다.
12. 희귀 미발견 어종은 15종 전까지 정확한 시간·날씨가 숨겨지는지 확인한다.
13. `?debug&fish=fish.catfish`, `fish.golden_koi`, `fish.coelacanth`로 희귀·영웅·전설 결과 배너·파티클·효과음을 확인한다.
14. 메뉴의 `낚시 장비`를 열어 기본 낚싯대 장착과 Lv.5/10/15·도감 20종 잠금 조건을 확인한다.
15. 사용 가능한 낚싯대를 선택한 뒤 메뉴 상태와 새로고침 후 장착 상태가 유지되는지 확인한다.
16. 낚시 결과의 +XP와 경험치 바가 100%까지 차고 레벨업 후 다음 레벨 진행도로 전환되는지 확인한다.
17. 발견 물고기를 눌러 중앙 팝업을 열고 X·Android 뒤로가기로 도감 목록에 복귀하는지 확인한다.
18. 결과창·도감 카드에 등급 글자 없이 어종 이름만 등급색으로 표시되고, 도감 상세에 등급 글자가 남는지 확인한다.
19. 레벨업 알림이 3초에는 유지되고 약 5초 후 사라지는지, X·뒤로가기·결과창 닫기로 즉시 사라지는지 확인한다.
20. 실제 기기에서 캐스팅 첨벙·획득 성공음의 음량과 겹침을 확인한다.
21. 가방의 물고기·장비 탭을 열고, 같은 어종을 2마리 낚아 물고기 카드 이미지 오른쪽 위에 `2` 배지가 표시되는지 확인한다. PC 커서·모바일 터치 시에만 이름이 보이는지도 확인한다.
22. 가방을 X·뒤로가기로 닫고 새로고침한 뒤 물고기 기록과 장착 장비가 유지되는지 확인한다.
23. 엘리에게 말을 걸면 판매창이 열리고, 어종별 수량·예상 금액·`전부` 선택이 동작하는지 확인한다. 하단 `나가기`로 닫은 뒤 다시 상호작용해 정상적으로 열리는지도 확인한다.
24. 일부만 판매하면 남은 개체 가격이 유지되고 코인·가방·도감이 올바른지 확인한다. 전부 판매·재접속·X·뒤로가기도 확인한다.
25. 판매 성공 후 사용자 제공 돈 소리와 판매창 상단 코인 증가·`+금액` 표시를 확인한다. 창을 즉시 닫거나 모션 감소를 켜도 최종 코인과 저장 데이터가 정확한지 확인한다. 판매 후 창을 닫았다가 다시 열 때 이전 `+금액`·코인 강조 연출이 재생되지 않는지 확인한다.
26. 마을 북쪽에서 숲, 동쪽에서 농장으로 이동하고 각 지역의 되돌아오는 출구를 확인한다. 재접속 시 마지막 지역과 위치가 복원되는지 확인한다.
27. 숲의 지정 나무를 세 번 때려 통나무가 2~4개 지급되고, 그루터기에서 반복 보상이 없는지 확인한다. 5분 후 또는 5분 이상 게임을 껐다 켠 뒤 나무가 다시 자라는지 확인한다.
28. 농장 4칸의 시작 밭, 잠긴 밭의 단계별 비용과 구매 후 영구 저장을 확인한다. 부족한 코인·통나무로는 구매되지 않아야 한다.
29. 엘리에게서 씨앗을 사서 빈 밭에 심고, 게임을 종료한 상태로 성장 시간을 넘긴 뒤 수확한다. 같은 밭에서 중복 수확되지 않아야 한다.
30. 가방의 재료 탭과 상점의 씨앗 구매·물고기/재료 판매 탭을 확인한다. 통나무·작물을 일부만 팔고 새로고침해 코인·잔량이 정확한지 확인한다.
31. 숲의 강과 농장 연못에서 낚시가 시작되고, 두 지역의 기본 어종이 서로 다른지 확인한다. 마을 연못의 임시 전체 어종 출현은 유지된다.
32. 숲의 벌목 나무 8종이 서로 구별되고, 벌목 후 대응 그루터기가 보이는지 확인한다. 엘리 노점·가방·밭 창에서 통나무와 씨앗 4종 PNG가 로드되는지 확인한다.

로컬 브라우저 화면과 자동 검사는 위 신규 항목을 통과했다. 실제 Android 기기에서의 손 조작·성능·음량은 별도 확인해야 한다.

최신 자동·모바일·배포 QA 결과는 저장소 루트의 `QA_REPORT.md`에 정리되어 있다.

## 7. 배포와 저장소

`main`에 push하면 `.github/workflows/deploy-pages.yml`이 다음 순서로 자동 배포한다.

```text
check → build → Pages artifact 업로드 → GitHub Pages 배포
```

배포 전에는 항상 아래 세 검사를 수행한다.

```bash
node scripts/check.mjs
node scripts/build.mjs
git diff --check
```

현재 `sidebatch/pixel-life` 하나가 공개 소스이자 배포 저장소다. 이전 별도 배포 저장소 `sidebatch/pixel-life-web`은 보관 처리되었으므로 다시 사용하지 않는다.

## 8. 변경 시 지켜야 할 규칙

- 모바일 이동 감각을 PC 수정과 함께 바꾸지 않는다.
- 월드 시간·날씨를 낚시 전용 코드로 옮기지 않는다.
- 새 어종·지역은 조건문 덩어리보다 데이터 파일로 추가한다.
- 통과 가능한 식생은 actor보다 아래에서 렌더링한다.
- 캐릭터와 건물 가림은 기존 Y-depth 규칙을 유지한다.
- 사용자 에셋과 기존 변경을 임의로 삭제하거나 덮어쓰지 않는다.
- 기능 완료 후 검사·빌드·모바일 화면 확인·Pages 성공 확인까지 수행한다.

## 9. 최근 핵심 커밋

```text
752e488  물고기 20종, 도감, 저장·복원, 전용 이미지와 누적 보상
4bcba73  현재 프로젝트 인수인계 문서 정리
0dfc713  낚시 중 가방·설정 표시 유지 및 터치 비활성화
8f53331  모바일 취소 고스트 클릭 수정, 시간·날씨 연출 개선
5b08b84  모바일 낚시 취소 UI 강조
8f50a25  낚싯줄·찌·입질 연출
672f638  100% 성공 낚시 기본 루프
767cce0  비·폭풍 밀도와 깊이 개선
366ccf3  비·폭풍 날씨 추가
3a5e5b5  시간대별 화면 효과
```
